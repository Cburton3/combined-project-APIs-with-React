export * from './grid-miembros';
// This line re-exports everything from grid-miembros.ts (inside the same folder) and makes it available through the index.ts file

// this creates a central export point for all contents within the folder. basically you can now import from otehr files everything in this folder -just add the export here. 